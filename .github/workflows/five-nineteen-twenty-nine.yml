name: five-nineteen-twenty-nine

on:
  schedule:
   - cron: '*/5 * * * *'
   - cron: '*/19 * * * *'
   - cron: '*/29 * * * *'     

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Run a one-line script
      run: |
        date "+Last updated on: %Y-%m-%d at %H:%M:%S"
